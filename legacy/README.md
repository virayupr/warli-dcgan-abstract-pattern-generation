# Legacy material

This directory preserves code and notebook material from the earlier
single-model study trained on 450 images. It is retained for provenance only
and does not reproduce the final DCGAN--WGAN-GP comparison.

For the submitted study, use the notebooks in the repository-level
`notebooks` directory and the outputs under `results/paper_ready`.
